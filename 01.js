// console.log("My name is prasidda dhakal");

// var: You can update its value and re-declare the same variable name multiple times in the same scope without errors.let: You can update (reassign) its value, but you cannot re-declare the same variable name in the same scope.const: You cannot reassign its value or re-declare it. It must get a value when you create it


let a = 5.01;
let b = 10;
let c = 2.02;
add = a + b + c;
console.log(add);
sub = a - b - c;
console.log(sub);
mul = a * b * c;
console.log(mul);
div = a / b / c;
console.log(div);
